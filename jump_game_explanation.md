# 跳跃游戏 II - 最小跳跃次数解法详解

## 问题描述

给定一个长度为 n 的 0 索引整数数组 nums。初始位置为 nums[0]。

每个元素 nums[i] 表示从索引 i 向后跳转的最大长度。换句话说，如果你在 nums[i] 处，你可以跳转到任意 nums[i + j] 处:
- 0 <= j <= nums[i] 
- i + j < n

返回到达 nums[n - 1] 的最小跳跃次数。生成的测试用例可以到达 nums[n - 1]。

## 核心解法：贪心算法

### 算法思路

使用贪心算法的核心思想是：**在每次跳跃中，选择能让下一跳到达最远位置的策略**。

### 关键变量

- `jumps`: 当前跳跃次数
- `current_end`: 当前跳跃能到达的最远位置
- `farthest`: 在当前跳跃范围内，下一跳能到达的最远位置

### Python3 实现

```python
def jump(nums):
    n = len(nums)
    if n <= 1:
        return 0
    
    jumps = 0          # 跳跃次数
    current_end = 0    # 当前跳跃能到达的最远位置
    farthest = 0       # 在当前跳跃范围内，下一跳能到达的最远位置
    
    # 遍历数组（不需要遍历最后一个元素，因为我们的目标就是到达它）
    for i in range(n - 1):
        # 更新在当前跳跃范围内，下一跳能到达的最远位置
        farthest = max(farthest, i + nums[i])
        
        # 如果已经到达了当前跳跃的边界
        if i == current_end:
            # 必须进行下一跳
            jumps += 1
            # 更新当前跳跃能到达的最远位置
            current_end = farthest
    
    return jumps
```

### 算法执行过程详解

以 `nums = [2,3,1,1,4]` 为例：

```
初始状态: jumps=0, current_end=0, farthest=0

i=0: nums[0]=2, 从位置0最远可到达2
     更新farthest: 0 -> 2
     到达跳跃边界(i==current_end)! 
     跳跃次数: 1, 新的边界: 2

i=1: nums[1]=3, 从位置1最远可到达4
     更新farthest: 2 -> 4

i=2: nums[2]=1, 从位置2最远可到达3
     更新farthest: 4 -> 4 (不变)
     到达跳跃边界(i==current_end)!
     跳跃次数: 2, 新的边界: 4

i=3: nums[3]=1, 从位置3最远可到达4
     更新farthest: 4 -> 4 (不变)

最终结果: 2 次跳跃
```

### 为什么贪心策略是正确的？

1. **局部最优导致全局最优**：在每个跳跃范围内，我们选择能让下一跳到达最远的位置
2. **最少跳跃次数**：这样能保证用最少的跳跃次数到达目标
3. **问题保证**：因为题目保证一定能到达终点，所以贪心策略是最优的

### 复杂度分析

- **时间复杂度**: O(n) - 只需要遍历数组一次
- **空间复杂度**: O(1) - 只使用了常数级别的额外空间

### 测试用例

| 输入 | 输出 | 解释 |
|------|------|------|
| [2,3,1,1,4] | 2 | 0→1→4 (跳1步到索引1，再跳3步到索引4) |
| [2,3,0,1,4] | 2 | 0→1→4 (跳1步到索引1，再跳3步到索引4) |
| [1,1,1,1] | 3 | 0→1→2→3 (每次只能跳1步) |

### 关键理解点

1. **边界概念**：`current_end` 表示当前跳跃能到达的最远边界
2. **贪心选择**：在到达边界之前，不断更新下一跳能到达的最远位置
3. **跳跃时机**：只有在到达当前边界时才进行跳跃
4. **终止条件**：不需要遍历最后一个元素，因为目标就是到达它

这个算法的优雅之处在于它将复杂的路径选择问题转化为简单的边界更新问题，通过贪心策略确保每次跳跃都能获得最大的收益。